describe("SfeirEventSheet", function() {
  
  var listByTypeInstance = new require('../hooks/listByType');

  it("and so is a spec", function() {
    console.log(listByTypeInstance);

    expect(a).toBe(true);
  });
});